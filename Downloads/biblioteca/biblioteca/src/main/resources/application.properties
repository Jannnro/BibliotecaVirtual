# Conexão com o banco
spring.datasource.url=jdbc:mysql://localhost:3306/biblioteca_virtual
spring.datasource.username=root
spring.datasource.password=nova123
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA / Hibernate
spring.jpa.hibernate.ddl-auto=validate
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.database-platform=org.hibernate.dialect.MySQLDialect
```

---

## Passo 6 — Testar se está funcionando

Execute o projeto clicando no botão ▶️ no IntelliJ. Se no console aparecer:

Started BibliotecaApplication in X seconds